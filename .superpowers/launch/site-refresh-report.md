# Site refresh — per-claim verification

Branch `site-refresh` off `main` @ `42d0989`. All commands verified against
a binary built fresh from that commit (`go build -o offshoot ./cmd/offshoot`,
darwin/arm64, go1.26.5) in a throwaway scratch store, not copy-pasted from
docs. Torture run and pytest tutorial were executed live, not cited from an
old report.

| Claim on the page | Source | Verified how |
|---|---|---|
| Module path `github.com/sricola/offshoot`, `go install .../cmd/offshoot@latest` resolves once public | `go.mod` line 1; `docs/status.md`'s "Standing nag" section | Read `go.mod`; read status.md's explicit statement that the retarget is done and `go install` "resolves today" (against the public repo) |
| Four tagged releases v0.1.0–v0.1.3, linux/darwin × amd64/arm64 binaries | `git tag -l`; `docs/eval-harness.md`'s Install section | `git tag -l` lists exactly `v0.1.0 v0.1.1 v0.1.2 v0.1.3`; eval-harness.md names the releases page and the two-OS/two-arch binary matrix |
| "Building from source is the fast path today... tagged releases carry prebuilt binaries" (teaser, not linked live) | Same as above + task's private-repo constraint | Repo is private (confirmed via existing page convention: all repo-pointing links are non-clickable `<span title=...>` teasers, never real `<a href>`) — followed the same convention for the new releases/benchmarks/demo-asset references, did not add live links into the private repo |
| Hero + quickstart CLI commands (`init`, `create`, `checkout`+SQL, `checkpoint`, `fork`, `rollback`, `promote`, `status`) | — | Ran every command verbatim, in order, against the fresh build in an empty scratch dir; all succeeded. **Found and fixed a real bug**: the pre-refresh hero terminal opened with `offshoot create app` (no `init` first) and inserted into `users` before creating the table — both fail against the real binary (`store: not found`, then `no such table: users`). Added the missing `offshoot init` row and merged `CREATE TABLE`+`INSERT` into one `sqlite3` call, mirroring the quickstart block's already-correct pattern |
| Daemon commands (`serve`, `session open`, `session flush`, `session status`, `fork --ttl`, `touch --ttl none`) | — (unchanged content, re-verified) | Ran the exact sequence shown in quickstart's daemon panel against a live `offshoot serve` background process; all succeeded, outputs matched the shown shape |
| `offshoot mcp` starts and speaks MCP; exposes exactly 7 tools | README's MCP section; `docs/status.md`'s "MCP server, 7 tools" row | Piped a real `initialize` + `tools/list` JSON-RPC exchange into `offshoot mcp` over stdio; got a valid `initialize` response and exactly `['offshoot_list','offshoot_checkout','offshoot_checkpoint','offshoot_fork','offshoot_rollback','offshoot_promote','offshoot_destroy']` (7) |
| MCP forks default TTL 24h; MCP rides a live daemon session when one's open | README "MCP" section; `docs/status.md` Integration surface rows | Read both docs' explicit statements (`-default-ttl` default `24h`; checkpoint/fork/checkout probe the daemon per call and use its live path when a session is already open) |
| pytest fixture plugin: `offshoot-db[pytest]`, `offshoot_daemon`/`offshoot_db`/`offshoot_fork` fixtures, seed-once-fork-many | `docs/eval-harness.md`; `sdk/python/offshoot/pytest_plugin.py` | Built a venv, `pip install -e "sdk/python[pytest]"`, wrote the tutorial's exact `pytest.ini`+seed+test file, ran `pytest -v` against the built binary — both tests passed live |
| TypeScript `testkit` (`@offshoot-db/client/testkit`), vitest/jest/`node:test` counterpart | `docs/eval-harness.md` (`startDaemon`/`seedOnce`/`forkPerTest`/`dump`); `docs/status.md` | Read both — not independently re-run (time-boxed); status.md's row is `shipped-and-tested` with a 22/22 `testkit.test.ts` count cited |
| Fork benchmark: 2.87s → ~198ms (512MB, local), fast path ~9.3ms size-independent | `docs/benchmarks.md`'s "Results: local store" table and "Isolating the fast path itself" section | Read the table directly (`ForkAtHead 512MB before 2.87s / after 198.2ms`) and the isolated-fast-path spot-check (`~9.3ms` at both 64MB and 512MB) |
| Fork benchmark: 4.57s → 1.03s (512MB, S3 via server-side `CopyObject`) | `docs/benchmarks.md`'s "Task 6b: S3 server-side CopyObject" table | Read the table directly (before-6b 4.57s, after-6b 1.030185084s) |
| Torture stats: 2,309 rounds / 230 capture-engine bounces / 230 resumed cleanly / 0 divergences | Live run, this session | `go test ./internal/capture -tags=torture -run TestTorture -count=1 -timeout 30m -v` — actual output: `torture complete: 2309 rounds, 230 bounces, 3 aggregate rebases across 231 session-starts, 230 resumed cleanly (resumed/bounce ratio: 1.00)`, `--- PASS`. Did **not** reuse the task brief's suggested 2,347/234 figures or the page's old 2,233/223 figures — torture round counts are wall-clock-bound and inherently non-deterministic between runs, so the page now says "latest run, not a historical best" and cites `make test-torture` to reproduce |
| Torture harness mechanics ("writer `kill -9`'d mid-write... capture engine bounced separately, gracefully") | `internal/capture/torture_test.go` (read directly) | The pre-refresh copy said "the capture process is killed mid-write," which is **wrong** — only the foreign `sqlite3` writer is SIGKILL'd (`rand.Intn(2)==0` branch); the capture engine itself is bounced via `cancel()` (graceful `ctx.Done()` shutdown), explicitly documented in-code as "NOT a process SIGKILL of the capturer." Rewrote the prose to describe the real mechanism instead of repeating the inaccuracy |
| pytest/testkit lead the "integration surface"; MCP TTL/daemon-riding; "operable" beat (metrics, HTTP, states, events, ro-cache budget, k8s sidecar) | `docs/status.md` Integration surface + Resource behavior + Platform sections; `docs/operations.md` (Metrics, Branch states, Eventing, Budgets sections); `docs/recipes/kubernetes.md` | Read all cited docs directly; six branch states (`active/dirty/detached/idle/pending/error`) confirmed against `docs/operations.md`'s Branch states table; metric/endpoint names (`GET /metrics`, `POST /rpc`, `/healthz`, `GET /events` SSE) confirmed against operations.md's Metrics/Eventing/HTTP sections |
| Honest limits: pre-release, Linux/macOS only, not a cluster, N×G storage cost with TTLs + reflink/CopyObject ("fast-fork") as mitigations, at-rest checkpoints are full snapshots | `docs/status.md` Platform section; `docs/faq.md`'s "Storage cost, honestly"; `ROADMAP.md`'s Non-goals (v1) | Read all three directly. `ROADMAP.md` non-goals explicitly names "TTLs + reflink + CopyObject" as the standing mitigation set before page-level dedupe — used that exact framing rather than the pre-refresh copy's TTL-only mitigation, which undersold what's already shipped |
| Demo assets exist: `docs/demo/parallel-attempts.cast`/`.txt`, MCP walkthrough | `docs/demo/README.md`, `ls docs/demo/` | Confirmed all four files present (`parallel-attempts.cast`, `.txt`, `mcp-walkthrough.md`, `mcp-session-driver.sh`); added teaser-style (non-clickable, same convention as source links) references from the "why" section |
| `examples/parallel-attempts/`, `examples/langgraph-rewind/` still exist | `ls examples/` | Confirmed both directories present, unchanged references |

## Also fixed while verifying (not explicitly requested, but load-bearing for the "verify every command" instruction)

- **Hero terminal bug**: pre-refresh commands failed against the real binary (missing `init`, missing `CREATE TABLE`). Fixed; added a 12th animation row to the reduced-motion-aware CSS to cover the new line.
- **Responsive bug, pre-existing and worsened**: inline `<code>` spans with no natural break points (bare URLs/paths like `github.com/sricola/offshoot/cmd/offshoot@latest`) overflowed their container on narrow viewports since `body` has `overflow-x:hidden` — text was silently clipped rather than wrapped. Pre-existed for one shorter S3-init line; my new, longer `go install` line made it worse. Fixed globally with `overflow-wrap:anywhere` on the base `code` rule (one-line, non-visual CSS addition, not a redesign).
- **4-column stats grid**: added responsive breakpoints (`900px`→2-col, `480px`→1-col) alongside the new fourth (fork) stat tile; verified the CSS cascade is correct by direct file inspection after a local headless-Chrome screenshot tool gave a false "2-column at 390px" reading — traced to a headless Chrome quirk (confirmed via a minimal repro: `window.innerWidth` reports ~500 regardless of a smaller `--window-size`/`--force-device-scale-factor=1` request), not a real CSS defect.

## Validation

- Custom Python `HTMLParser`-based tag-matching check: no mismatched/unclosed tags, before and after edits.
- `html5lib` strict parse: zero new errors (two pre-existing, unrelated warnings on line 11's Google Fonts URL, unchanged by this diff).
- All new `&`/`<`/`>` in the diff confirmed properly escaped (`&amp;`, `&nbsp;`) via `git diff` grep.
- Rendered full-page and mobile-width screenshots via headless Chrome to sanity-check the 4-stat strip, restructured cards, two-column code split, and honest-limits section.
